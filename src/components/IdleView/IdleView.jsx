import styles from "./IdlePage.module.css";

const IdleView = () => {
  return (
    <h3 className={styles.mainText}>
      To find images input image description in search field and press ENTER (or
      search button)
    </h3>
  );
};

export default IdleView;
